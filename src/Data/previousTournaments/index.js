// Pseudocode
/*

  Step One: Collect all directories in this folder
  
  Step Two: Iterate over all directories and create an Tournament({}) Object
    TournamentObject({
      EventTitle: value,
      PathToPublicNorm: value,
      PathToPublicTourn: value
    })

  Step Three: Export a Large Array Containing all created objects

  // IN <LayoutSelection>:

  Step Four: Import Previous Array

  Step Five: Iterate over Array and create <option>{EventTitle}</option> from array

  Step Six: Enclose previous <option>... w/ <Link to={PathToPublic}>

  (((  Maybe Create a bunch of "<LayoutScores>" ??? ^^^^^^^^^^^^^^^^^^^^ )))
*/